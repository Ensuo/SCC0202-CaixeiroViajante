#include <stdio.h>
#include "lista.h"

int main(){
    int n;

    lista* cidades = lista_criar();
    int cidades_passadas[12];

    for(int i = 0 ; i < 12; i++){
        cidades_passadas[i] = -1;
    }

    for(int i = 0; i < n; i++){
        int a, b, distancia;
        scanf("%d %d %d", &a, &b, &distancia);
        
    }

    return 0;
}