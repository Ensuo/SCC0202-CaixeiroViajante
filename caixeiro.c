#include <stdio.h>
#include "lista.h"

int main(){
    int n;

    list* cidades = lista_criar();

    for(int i = 0; i < n; i++){
        int a, b, distancia;
        scanf("%d %d %d", &a, &b, &distancia);
    }

    return 0;
}